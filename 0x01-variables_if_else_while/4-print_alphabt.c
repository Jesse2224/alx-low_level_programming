#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - Entry point
* Description: using the main function
* this program prints "Programming is positive, zero, or negative
* Return: 0
*/
int main(void)
{
char ch, e, q;

	e = 'e';
	q = 'q';

for (ch = 'a'; ch <= 'z'; low++)
{
if (ch != e && ch != q)
{
	putchar(ch);
}
}
printf("\n");
return (0);
}
