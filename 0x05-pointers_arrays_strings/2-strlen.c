#include<stdio.h>
#include "main.h"

/*
*_strlen - returns the length of a string..
*@s: value to be evaluate.
*Return: not.
*/

void _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
